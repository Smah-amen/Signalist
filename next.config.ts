import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // enable turbopack with an options object (adjust properties as needed)
  turbopack: {

  },
  /* config options here */
};

export default nextConfig;
